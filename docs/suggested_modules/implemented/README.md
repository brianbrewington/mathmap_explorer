# Implemented Proposals (Archive)

Original proposals for explorations that have since been implemented in
`js/explorations/`. Kept for historical context and to track the
proposal-to-implementation pipeline. Do not file new proposals here — use
the parent `docs/suggested_modules/` directory for those.

When implementing a proposal, move its markdown file into this folder.
